{
  "name": "iotex-dashboard-backend",
  "version": "1.0.0",
  "description": "Backend for the IoTeX Analytics Dashboard",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fetch-frontend": "./fetch_frontend",
    "compile": "npx tsc --project ./tsconfig.json",
    "start": "npm run fetch-frontend && npm run compile && nodemon dist/index.js"
  },
  "author": "Ritchel Cousar Jr.",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.9"
  },
  "devDependencies": {
    "@types/express": "^4.17.12",
    "typescript": "^4.3.5"
  }
}
