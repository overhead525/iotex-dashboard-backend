{
  "name": "iotex-dashboard-backend",
  "version": "1.0.0",
  "description": "Backend for the IoTeX Analytics Dashboard",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fetch-frontend": "./fetch_frontend",
    "compile": "npx tsc --project ./tsconfig.json",
    "start": "npm run compile && node dist/index.js",
    "production": "npm run fetch_frontend && npm run start"
  },
  "author": "Ritchel Cousar Jr.",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.12",
    "typescript": "^4.3.5"
  }
}
